# PlugLauncher — لیست تسک‌ها

اپ لانچر سبک ویندوزی با معماری پلاگین‌محور (پلاگین‌ها به‌صورت اسکریپت `.csx` نوشته می‌شوند).

**استک:** .NET 10 · WPF · Roslyn Scripting (`Microsoft.CodeAnalysis.CSharp.Scripting`)

---

## فاز ۰ — راه‌اندازی

- [x] **T1** ساخت ساختار سولوشن و پروژه‌ها (`Contracts` / `Core` / `App`) + git init
- [x] **T2** تعریف قرارداد پلاگین: `IPlugin`، `PluginResult`، `IPluginContext`، `PluginManifest`

## فاز ۱ — هسته‌ی پلاگین

- [x] **T3** مدل مانیفست + کشف پلاگین‌ها از پوشه‌ی `plugins/` (اسکن، پارس `plugin.json`، اعتبارسنجی)
- [x] **T4** لودر CSX با Roslyn + کش کامپایل بر اساس hash فایل
- [x] **T5** اجرای کوئری روی پلاگین‌ها (async، timeout، ایزوله‌سازی خطا تا یک پلاگین خراب کل اپ را نخواباند)
- [x] **T6** ذخیره‌سازی آمار استفاده (recency/frequency) در `%APPDATA%\PlugLauncher\usage.json`

## فاز ۲ — رابط کاربری

- [x] **T7** پنجره‌ی اصلی WPF: بدون border، افکت شیشه‌ای/blur، Topmost، وسط صفحه
- [x] **T8** هات‌کی سراسری (`RegisterHotKey`) برای نمایش/مخفی‌سازی + بستن با `Esc` و از دست دادن فوکوس
- [x] **T9** باکس جستجو + auto-complete درون‌خطی (متن خاکستری ادامه‌ی تایپ، مثل تصویر مرجع)
- [x] **T10** لیست نتایج (آیکن + عنوان + زیرعنوان) با ناوبری کیبورد و اجرا با Enter/کلیک
- [x] **T11** حالت پیش‌فرض (کوئری خالی): لیست پلاگین‌ها به ترتیب آخرین استفاده + دکمه‌ی تنظیمات
- [x] **T12** صفحه‌ی تنظیمات: مدیریت پلاگین‌ها (فعال/غیرفعال، حذف، باز کردن پوشه، تغییر هات‌کی)
- [x] **T13** تم/استایل: بارگذاری استایل از پوشه‌ی `theme/` + پشتیبانی از `assets/` پلاگین‌ها

## فاز ۳ — پلاگین نمونه و اعتبارسنجی

- [x] **T14** نوشتن ۲ پلاگین نمونه‌ی CSX (مثلاً Steam Games و Calculator) برای اعتبارسنجی کل مسیر
- [x] **T15** تست دستی end-to-end + پاک‌سازی build warningها

## فاز ۴ — فروشگاه پلاگین (روی VPS)

- [x] **T16** طراحی فرمت بسته‌ی پلاگین — `.plz` (همان zip) با `plugin.json` در ریشه؛ اعتبارسنجی کامل سمت سرور
  (zip slip، سقف تعداد فایل/حجم/حجم بازشده، مانیفست، وجود فایل ورودی و آیکن)؛ `sha256` سمت سرور محاسبه و
  سمت کلاینت بررسی می‌شود. ابزارها: `tools/pack-plugin.ps1` و `tools/publish-plugin.ps1`.
- [x] **T17** API فروشگاه روی VPS — پروژه‌ی `src/PlugLauncher.Store` (ASP.NET Core Minimal API، بدون دیتابیس؛
  فهرست از روی ساختار دیسک ساخته می‌شود). زنده روی `https://thehokm.cloud/pluglauncher/`؛ سرویس systemd با
  کاربر اختصاصی و `ProtectSystem=strict`، پشت nginx. **بازنشسته شد (۱۶ شهریور ۱۴۰۵)** — فروشگاه
  به GitHub Pages منتقل و هم پروژه و هم سرویس حذف شدند؛ پایین را ببین.
- [x] **T18** کلاینت فروشگاه داخل اپ — `StoreClient` در Core (جستجو، دانلود، بررسی sha256، استخراج امن) +
  تب «فروشگاه» در صفحه‌ی تنظیمات با نصب/به‌روزرسانی و آدرس قابل تغییر سرور.

---

## ساختار پوشه‌ی یک پلاگین

```
plugins/
  my-plugin/
    plugin.json      # مانیفست: id, name, version, entry, keyword, icon
    main.csx         # کد پلاگین (نقطه‌ی ورود)
    assets/          # آیکن‌ها و فایل‌های استاتیک
    theme/           # استایل اختیاری مخصوص پلاگین
```

---

## وضعیت فعلی (۱۴ شهریور ۱۴۰۵ / 2026-09-06)

فازهای ۰ تا ۳ تمام شده و روی سیستم تست شده است:

- سه پلاگین نمونه (`steam-games`، `programs`، `calculator`) کامپایل و لود می‌شوند.
- زمان لود: **۱۶۲۰ms** بار اول (کامپایل Roslyn) → **۳۶ms** دفعات بعد (کش دیسکی).
- هات‌کی `Alt+Space`، گرفتن فوکوس، لیست پیش‌فرض، جستجو، آیکن‌ها و تکمیل درون‌خطی همگی روی
  اسکرین‌شات واقعی تأیید شدند.
- `dotnet build` روی کل سولوشن: ۰ خطا، ۰ هشدار.

### فاز ۴ — تمام شد (۱۵ شهریور ۱۴۰۵ / 2026-09-06)

فروشگاه روی `https://thehokm.cloud/pluglauncher/` بالا است و هر سه بسته‌ی نمونه روی آن منتشر شده‌اند.
مسیر کامل تست شد: `pack → publish → لیست از روی HTTPS → دانلود → بررسی sha256 → استخراج در
%APPDATA%\PlugLauncher\plugins → لود شدن پلاگین در اپ`. نصب با sha256 دستکاری‌شده هم عمداً امتحان شد و
رد شد. سایت اصلی بازی روی همان دامنه دست‌نخورده ماند (`GET /` همچنان 200).

**تست‌نشده (نیازمند بازبینی چشمی روی دسکتاپ)**: خود تب «فروشگاه» در UI — کد آن بیلد می‌شود و منطقش از
همان `StoreClient` تست‌شده استفاده می‌کند، ولی نصب از طریق دکمه‌ی UI به‌صورت بصری اجرا نشده است.

### صفحه‌ی مرور در مرورگر (۱۵ شهریور ۱۴۰۵)

باز کردن `https://thehokm.cloud/pluglauncher/` در مرورگر ۴۰۴ می‌داد. علتش nginx نبود (`/health` و
`/api/v1/plugins` هر دو ۲۰۰ می‌دادند) بلکه این بود که سرویس فقط API بود و هیچ روتی روی `/` نداشت.
`IndexPage.cs` اضافه شد: یک صفحه‌ی HTML بدون جاوااسکریپت و بدون `wwwroot` که فهرست بسته‌ها را با
آیکن و لینک دانلود نشان می‌دهد.

یک نکته سر راه: نسخه‌ی اول تگ `<base href="/">` می‌گذاشت، چون nginx با `proxy_pass` اسلش‌دار خودش
prefix را حذف می‌کند و برنامه `PathBase` خالی می‌بیند — نتیجه این بود که لینک‌های دانلود به ریشه‌ی
دامنه می‌رفتند. تگ `base` حالا فقط وقتی نوشته می‌شود که `PathBase` واقعاً مقدار داشته باشد.

### انگلیسی‌سازی + ظاهر تب‌ها + نسخه‌ی ۱.۱.۰ (۱۵ شهریور ۱۴۰۵)

- **تب‌ها**: دو استایل `SegmentedTabItem`/`SegmentedTabControl` به `Themes/Dark.xaml` اضافه شد — نوار
  گِرد تیره که فقط به اندازه‌ی خود تب‌هاست و تب فعال یک قرص روشن‌تر با متن SemiBold می‌شود.
  جایگزین `TabControl` پیش‌فرض ویندوز که با تم تیره جور نبود.
- **زبان**: کل رشته‌های قابل مشاهده انگلیسی شد — XAML هر دو پنجره، پیام‌های `MessageBox`، متن‌های
  استثنا و **تمام خطوط لاگ**. `FlowDirection="RightToLeft"` از پنجره‌ی تنظیمات برداشته شد.
  کامنت‌های داخل کد عمداً فارسی ماندند (برای توسعه‌دهنده‌اند، نه کاربر). صفحه‌ی وب فروشگاه هم
  انگلیسی و `lang="en"` شد.
- **پلاگین‌ها**: نام و توضیح هر سه انگلیسی شد (`Calculator` / `Programs` / `Steam Games`) و نسخه
  به `1.1.0` رفت. هر سه روی فروشگاه منتشر شدند (اکنون `versions=6`؛ نسخه‌ی `1.0.0` هم سر جایش است).
- **چیدمان تست آپدیت**: قبل از ویرایش، نسخه‌ی ۱.۰.۰ هر سه پلاگین در
  `%APPDATA%\PlugLauncher\plugins` گذاشته شد. چون ریشه‌ی کاربر بر پلاگین‌های همراه برنامه اولویت
  دارد، اپ ۱.۰.۰ فارسی را لود می‌کند و تب Store هر سه را با دکمه‌ی **Update** نشان می‌دهد.

**تأیید بصری**: پنجره‌ی تنظیمات با اسکرین‌شات بررسی شد — تب‌های سگمنتی درست رندر می‌شوند، متن‌ها
انگلیسی و چپ‌به‌راست‌اند، و تب Store هر سه بسته را به شکل `v1.1.0 · installed v1.0.0` با دکمه‌ی
Update فهرست می‌کند (آیکن Steam هم از فروشگاه دانلود شد). **خودِ زدن دکمه‌ی Update تست نشد**:
کلیک مصنوعی (`mouse_event`) به این پنجره نمی‌رسد و `UIAutomation` هم پنجره را نمی‌بیند؛ فقط ورودی
کیبورد کار می‌کند و رسیدن با Tab به دکمه‌های داخل `ItemsControl` جواب نداد. مسیر نصب
(`StoreClient.InstallAsync`) قبلاً به‌صورت end-to-end تست شده است.

### پلاگین جدید: Password Generator (۱۵ شهریور ۱۴۰۵)

`plugins/password/` — کلیدواژه‌ی `pw`، نسخه‌ی ۱.۰.۰، منتشرشده روی فروشگاه (اکنون ۴ بسته).

پارامترها بعد از کلیدواژه، به هر ترتیب و بدون حساسیت به بزرگی/کوچکی حروف:

| ورودی | معنی |
|---|---|
| عدد | طول، محدود به ۴ تا ۱۲۸ (خارج از بازه clamp می‌شود، نه رد) |
| `nosym` / `ns` / `alnum` | بدون کاراکتر خاص |
| `nu` / `nl` / `nd` | حذف حروف بزرگ / کوچک / ارقام |
| `easy` | حذف کاراکترهای هم‌شکل (`O 0 o I l 1 | S 5 B 8 Z 2`) |
| `pin` | فقط رقم |
| `hex` | فقط هگز کوچک |

نکته‌های پیاده‌سازی:
- **`RandomNumberGenerator.GetInt32`** همه‌جا، نه `System.Random` — هم CSPRNG است و هم بازه‌اش
  بدون بایاس (modulo bias ندارد).
- از هر مجموعه‌ی انتخاب‌شده **حداقل یک کاراکتر** تضمین می‌شود، بعد بقیه از اجتماع کشیده و کل رشته
  با Fisher-Yates بُر می‌خورد تا کاراکترهای تضمینی همیشه اول رشته نباشند.
- اگر کاربر همه‌ی مجموعه‌ها را حذف کند (`pw 20 nu nl nd ns`) به‌جای خطا، حروف کوچک برمی‌گردد.
- سه گزینه در هر جستجو تولید می‌شود تا اگر شکل یکی ناخوشایند بود، بعدی انتخاب شود. آنتروپی هر
  کدام (`~bits`) در زیرنویس می‌آید.
- گزینه‌ی ناشناخته (`pw 12 bogus`) به‌جای تولید رمز، ردیف راهنما نشان می‌دهد.
- `Clipboard.SetText` سه بار تلاش می‌کند؛ کلیپ‌بورد ویندوز گاهی در تصاحب برنامه‌ی دیگری است.

**تست**: پلاگین با **لودر واقعی** (`CsxPluginLoader` روی همان فایل `main.csx`) در یک پروژه‌ی موقت
اجرا شد — ۱۰ حالت پارامتری بررسی و ۶۰۰۰ رمز تولید شد: همه با طول درست و **۶۰۰۰ مقدار یکتا**.
داخل خود لانچر هم لود شد (`loaded 4 of 4 plugins`). اسکرین‌شات از نتیجه در لانچر گرفته نشد چون
`Alt+Space` وقتی پنجره باز است آن را می‌بندد و پنجره با از دست دادن فوکوس مخفی می‌شود.

### آدرس فروشگاه، آیکن‌ها و کشف‌پذیری گزینه‌های رمز (۱۵ شهریور ۱۴۰۵)

- [x] **فیلد «Store URL» از تب فروشگاه برداشته شد.** مقدار همچنان در `AppSettings.StoreUrl`
  هاردکد است (`https://thehokm.cloud/pluglauncher`) و فقط با ویرایش دستی
  `%APPDATA%\PlugLauncher\settings.json` عوض می‌شود. `PluginEngine.SetStoreUrl` هم حذف شد چون
  دیگر فراخوانی نداشت. زیرنویس تب همین را می‌گوید تا اگر کسی لازم داشت بداند کجا را عوض کند.
  دلیل: یک تکست‌باکس آزاد در UI یعنی کاربر می‌تواند ناخواسته به مخزن دیگری وصل شود، و کد پلاگین
  بدون sandbox اجرا می‌شود — پس منبع بسته‌ها نباید یک کلیک فاصله داشته باشد.

- [x] **آیکن برای Calculator و Programs.** `tools/make-icons.ps1` هر دو را می‌سازد (۱۲۸×۱۲۸،
  همان دستور آیکن قفل: پلاک سرمه‌ای `#222A3A` با گِردی ۲۲، گلیف تخت فیروزه‌ای `#5CC8D7`، بدون
  گرادیان). ماشین‌حساب: بدنه‌ی فیروزه‌ای که نمایشگر و ۹ دکمه‌اش با رنگ پس‌زمینه از تویش بریده شده.
  برنامه‌ها: شبکه‌ی ۲×۲ کاشی، کاشی آخر کم‌رنگ‌تر (`#3A7C88`) تا «و بقیه» را برساند.
  اسکریپت نگه داشته شد چون آیکن قفل اسکریپت نداشت و دوباره‌سازی‌اش قابل تکرار نبود.

- [x] **گزینه‌های پلاگین رمز حالا قابل کشف‌اند.** قبلاً فقط یک ردیف راهنمای فشرده بود
  (`pw 24 nosym easy`) که معنی توکن دومش معلوم نبود. راه‌حل: یک قابلیت کوچک در قرارداد —
  `PluginResult.ReplaceQuery`. ردیفی که این را پر کند کاری اجرا نمی‌کند؛ Enter روی آن فقط متن
  باکس جستجو را با مقدارش عوض می‌کند و پنجره باز می‌ماند (`MainWindow.ActivateSelectedAsync`،
  دقیقاً کنار همان شاخه‌ای که برای ردیف پلاگین کلیدواژه‌اش را در باکس می‌گذارد).

  پلاگین رمز حالا **۵ ردیف نمونه** نشان می‌دهد، هر کدام با یک جمله‌ی توضیح و طول فعلی کاربر
  داخلش (`pw 24 nosym` وقتی کاربر روی ۲۴ است، نه `pw 16 nosym`). نمونه‌ها تا وقتی هیچ گزینه‌ای
  استفاده نشده روی صفحه می‌مانند — یعنی هم `pw` و هم `pw 24` نحو را یاد می‌دهند — و به‌محض
  استفاده از یک گزینه کنار می‌روند. ۳ رمز + ۵ نمونه = ۸ ردیف، دقیقاً برابر `MaxResults`.
  در حالت گزینه‌ی ناشناخته هم به‌جای بن‌بست، همین نمونه‌ها زیر پیام خطا می‌آیند.

  `description` مانیفست هم از `pw [length] [options]` به یک جمله‌ی مشخص عوض شد؛ همان `[options]`
  مبهم بود که در فهرست پلاگین‌ها دیده می‌شد.

**نسخه‌ها**: Calculator و Programs به `1.2.0` (آیکن) و Password به `1.1.0` (نمونه‌ها) رفتند و هر
سه روی فروشگاه منتشر شدند — الان `plugins=4, versions=10` و هر چهار بسته آیکن دارند
(`/api/v1/plugins/{id}/icon` برای هر چهار تا ۲۰۰ و `image/png` برمی‌گرداند).

**تست‌های خودکار**: `dotnet build` روی ۵ پروژه ۰ خطا/۰ هشدار. پلاگین رمز دوباره با
`CsxPluginLoader` واقعی در پروژه‌ی موقت اجرا شد: ۱۰ حالت پارامتری درست، ردیف‌های `ReplaceQuery`
مقدار درست دارند، و ۶۰۰۰ رمز ۲۰ کاراکتری همه با طول درست و **۶۰۰۰ مقدار یکتا**.

**تست‌های UI (دستی — با کاربر)**: از این به بعد تست‌های رابط کاربری را کاربر خودش انجام می‌دهد و
اینجا فقط چک‌لیستشان نوشته می‌شود. برای این تغییرات:

1. تنظیمات → تب **Store**: فیلد آدرس و دکمه‌ی Save URL نباید دیده شوند؛ لیست باید بلافاصله زیر
   نوار جستجو شروع شود.
2. همان تب: Calculator و Programs باید `v1.2.0 · installed v1.1.0` و Password باید
   `v1.1.0 · installed v1.0.0` با دکمه‌ی **Update** نشان دهند. هر سه را Update کنید.
3. بعد از Update، آیکن ماشین‌حساب و شبکه‌ی ۲×۲ باید کنار نامشان در لیست فروشگاه بیاید.
4. `Alt+Space` → `pw`: سه رمز + پنج ردیف نمونه.
5. روی ردیف `pw 24 easy` (یا هر نمونه‌ی دیگر) Enter بزنید: پنجره **نباید بسته شود**؛ متن باکس
   باید به همان مقدار عوض شود و رمزهای جدید با همان گزینه تولید شوند.
6. `pw 12 bogus`: پیام «Unknown option» در بالا و نمونه‌ها زیرش.
7. Enter روی خود رمز: کپی در کلیپ‌بورد و بسته شدن پنجره.

### تشخیص چیدمان اشتباه کیبورد (۱۵ شهریور ۱۴۰۵)

- [x] **وقتی زبان ویندوز روی فارسی مانده، متن تایپ‌شده خودکار خوانده می‌شود.** کاربر `chrome`
  می‌زند ولی چیزی که در باکس می‌نشیند `زاقخپث` است. `KeyboardLayout` (در `PlugLauncher.Core`)
  هر حرف را به حرفی که روی همان کلید فیزیکی است برمی‌گرداند. جدول از چیدمان استاندارد فارسی
  ویندوز (kbdfa) آمده: سه ردیف اصلی حرف‌به‌حرف، به‌علاوه‌ی حرف‌های Shift دار
  (`ژ`→`c`، `آ`→`h`، `ئ`→`s`، …) و ارقام فارسی/عربی (`۲۴`→`24`).

  تبدیل **دوطرفه** است: اگر متن حرف فارسی داشته باشد فارسی→انگلیسی خوانده می‌شود، وگرنه
  انگلیسی→فارسی (برای وقتی که کاربر می‌خواست فارسی بنویسد و کیبورد انگلیسی بود). ارقام انگلیسی
  عمداً به فارسی تبدیل نمی‌شوند: `24` در جستجو تقریباً همیشه یعنی همان عدد.

- [x] **قاعده‌ی فعال شدن: فقط وقتی نتیجه‌ای نیست.** `PluginEngine.QueryAsync` اول با متن خود
  کاربر همه‌ی پلاگین‌ها را صدا می‌زند؛ **اگر و فقط اگر** خروجی خالی بود، یک‌بار دیگر با متن
  اصلاح‌شده صدا می‌زند. پس این قابلیت هیچ‌وقت جای یک نتیجه‌ی واقعی را نمی‌گیرد و روی جستجوی
  فارسیِ عمدی (اسم فارسی یک برنامه یا فایل) اثر ندارد. چون کل مسیر دوباره اجرا می‌شود،
  کلیدواژه‌ی پلاگین‌ها هم درست تشخیص داده می‌شود (`حص ۲۴` → `pw 24`).

  خروجی `QueryAsync` از `IReadOnlyList<SearchItem>` به `QueryOutcome` (نتایج + متن اصلاح‌شده)
  عوض شد تا UI بداند چه چیزی واقعاً جستجو شده؛ تنها فراخوان‌کننده‌اش `MainWindow` بود.

- [x] **نمایش در UI بدون تغییر XAML.** همان لایه‌ی متن خاکستریِ تکمیل خودکار استفاده شد:
  وقتی اصلاحی در کار باشد، به‌جای ادامه‌ی عنوانِ ردیفِ انتخاب‌شده، `   →  chrome` نشان داده
  می‌شود. **Tab** هم در این حالت به‌جای چسباندن ادامه، خودِ متن باکس را با متن درست عوض می‌کند.
  متن تایپ‌شده دست‌کاری نمی‌شود مگر خود کاربر Tab بزند — نوشتن خودکار در باکس وسط تایپ کردن
  با کاربر می‌جنگد.

  هر بار که این مسیر فعال شود یک خط در لاگ می‌آید:
  `keyboard layout fix: "حص" read as "pw" (8 results)`.

**تست‌های خودکار**: `dotnet build` روی ۵ پروژه ۰ خطا/۰ هشدار. یک پروژه‌ی کنسول موقت با ارجاع به
`PlugLauncher.Core` واقعی: ۱۲ حالت جدول (رفت و برگشت `chrome`/`زاقخپث`، `سلام`/`sghl`،
`حص ۲۴`→`pw 24`، و رد کردن `24` و متن خالی) و ۶ رفت‌وبرگشت کامل — همه درست. بعد با موتور و
پلاگین‌های واقعی: `حص` → ۸ ردیف رمز، `حص ۲۴` → رمزهای ۲۴ کاراکتری، و `pw` (که خودش نتیجه دارد)
بدون هیچ اصلاحی.

**تست‌های UI (دستی — با کاربر)**:

1. `Alt+Space`، زبان کیبورد را فارسی کنید و `pw` بزنید (یعنی `حص`): باید سه رمز بیاید و کنار
   متنِ فارسی، خاکستری بنویسد `→ pw`.
2. همان‌جا **Tab** بزنید: متن باکس باید به `pw` تبدیل شود و نشانگر آخرش برود.
3. `حص ۲۴` (یعنی `pw 24`): رمزهای ۲۴ کاراکتری.
4. یک اسم برنامه‌ی واقعی را با کیبورد فارسی بزنید (مثلاً `زاقخپث` برای chrome، `فثمثلقشپ` برای
   telegram): باید همان برنامه پیدا شود و Enter اجرایش کند.
5. یک عبارت فارسیِ واقعی که برنامه‌ای با آن اسم هست را بزنید: نباید چیزی به هم بریزد — اصلاح
   فقط وقتی می‌آید که هیچ نتیجه‌ای نباشد.
6. متن باکس را خالی کنید: متن خاکستری هم باید برود.

### فروشگاه بدون سرور — سمت کلاینت (۱۵ شهریور ۱۴۰۵، برنچ `feature/static-store`)

هدف: بشود فروشگاه را روی یک هاست استاتیک (GitHub Pages و مثل آن) گذاشت، **بدون اینکه کاربر به
git یا هر ابزار دیگری نیاز پیدا کند**. کاربر فقط دو `GET` روی HTTPS می‌زند؛ git فقط ابزار انتشار
است و آن هم فقط سمت ما.

- [x] **`StoreClient` هر دو نوع فروشگاه را می‌فهمد.** اول `index.json` از ریشه‌ی آدرس، و اگر
  ۴۰۴ داد برگشت به `api/v1/plugins`. نتیجه‌ی تشخیص در همان کلاینت کش می‌شود، پس روی VPS فقط
  یک ۴۰۴ اضافه در اولین بار خورده می‌شود. اگر بعداً یک `index.json` هم کنار API سرو شود، همان
  یک درخواست هم می‌افتد.

- [x] **خواندن بدنه به‌صورت رشته به‌جای `GetFromJsonAsync`.** آن متد پاسخ را با Content-Type
  غیر از `application/json` رد می‌کند و هاست‌های استاتیک (مثلاً `raw.githubusercontent.com`)
  برای JSON هم `text/plain` می‌فرستند. حالا هم آرایه‌ی خام و هم پاسخ صفحه‌بندی‌شده پذیرفته
  می‌شود.

- [x] **فیلتر جستجو به سمت کلاینت آمد** (روی id/name/description/keywords)، چون هاست استاتیک
  `?q=` ندارد. قبلاً هم عملاً کل فهرست دانلود می‌شد، پس هزینه‌ی شبکه فرقی نکرد.

- [x] **`tools/build-static-store.ps1`**: از روی پوشه‌ی `.plz` ها یک فروشگاه استاتیک می‌سازد —
  `index.json` (فقط آخرین نسخه‌ی هر بسته، با `sha256` و `size` محاسبه‌شده از خود فایل) و
  `packages/{id}/{version}/…` برای همه‌ی نسخه‌ها به‌علاوه‌ی آیکنِ بیرون‌کشیده از داخل بسته.
  آدرس‌ها نسبی‌اند تا زیر هر prefix ای کار کنند.

  دو نکته‌ی ویندوزی که وقت گرفتند: اسکریپت باید **BOM** داشته باشد وگرنه PowerShell 5.1 آن را
  ANSI می‌خواند و روی متن فارسی خطای پارس می‌دهد؛ و `ZipArchiveEntry.FullName` روی ویندوز نام
  ورودی‌ها را با بک‌اسلش برمی‌گرداند در حالی که مانیفست اسلش دارد، پس مقایسه باید یکدست‌سازی شود
  (بدون این، آیکن هیچ بسته‌ای پیدا نمی‌شد).

**تست‌های خودکار**: یک سرور استاتیک محلی که عمداً برای JSON هم `text/plain` می‌فرستد + پروژه‌ی
کنسول موقت با `PlugLauncher.Core` واقعی — ۱۲ بررسی، همه سبز:
فهرست ۴ بسته با `sha256` دقیقاً برابر مقادیر منتشرشده روی VPS، جستجو با نام/کلیدواژه/بی‌نتیجه،
دانلود آیکن (۱۸۹۰ بایت)، **نصب واقعی** زیر یک شناسه‌ی موقت با بررسی sha، **رد شدن بسته‌ی
دست‌کاری‌شده** (sha جعلی) بدون جا گذاشتن پوشه، و در کنارش همان کلاینت روی VPS فعلی
(`https://thehokm.cloud/pluglauncher`) که ۴ بسته‌ی درست را برمی‌گرداند. آدرس بی‌جواب هم پیام
روشن می‌دهد نه استثنای خام.

**هاستینگ — تصمیم گرفته شد و پیاده شد (۱۵ شهریور ۱۴۰۵)**: GitHub Pages روی همین مخزن، برنچ
یتیم `gh-pages` (ریشه)، آدرس `https://mul83rry.github.io/PlugLauncher/`. دسترسی `github.io` از
شبکه تست شد و جواب داد، پس سراغ `cdn.jsdelivr.net/gh/...` نرفتیم. برنچ `gh-pages` در worktree
جدا (`../PlugLauncher-pages`) ساخته می‌شود تا درخت کاری اصلی و تاریخچه‌ی سورس با فایل‌های باینری
`.plz` قاطی نشود. `.nojekyll` هم کنار `index.json` هست وگرنه Pages هر چیزی را که با `_` شروع شود
نادیده می‌گیرد. (چک‌لیست انتشار عمومی که این‌جا به آن ارجاع می‌شد انجام و حذف شد.)

`publish-plugin.ps1` بازنویسی شد: حالت پیش‌فرضش «کپی + push» است (بسته → `dist` →
`build-static-store.ps1` → worktree ی `gh-pages` → commit/push، بدون کلید و بدون سرور) و مسیر
قدیمی `POST` با `X-Api-Key` پشت سوییچ `-Api` برای برگشت به VPS نگه داشته شد. سراغ Contents API
با PAT نرفتیم چون ماشین منتشرکننده گیت دارد.

آدرس پیش‌فرض کلاینت هم به Pages منتقل شد (`AppSettings.DefaultStoreUrl`). چون نصب‌های موجود
آدرس قدیمی را در `settings.json` ذخیره دارند، `SettingsStore.Load` یک ارتقای یک‌بارمصرف دارد:
فقط اگر مقدار دقیقاً برابر `LegacyStoreUrl` باشد جابه‌جا و فایل دوباره نوشته می‌شود.

**مسیر VPS از کلاینت حذف شد (۱۵ شهریور ۱۴۰۵)**: بعد از اینکه فروشگاه روی Pages درست کار کرد،
منطق دوحالته‌ی `StoreClient` برداشته شد. حالا فقط `index.json` خوانده می‌شود و اگر نبود همان‌جا
خطای روشن می‌دهد؛ دیگر نه `api/v1/plugins` صدا زده می‌شود و نه آدرس دانلود از روی شناسه‌ی بسته
ساخته می‌شود (اگر `downloadUrl` در فهرست نباشد، خطا). `publish-plugin.ps1` هم سوییچ `-Api` و
هدر `X-Api-Key` را از دست داد و فقط مسیر گیت می‌ماند. `docs/STORE.md` بازنویسی شد و همه‌ی
بخش‌های سرور (API، چیدمان systemd/nginx، آی‌پی و ssh alias، روش به‌روزرسانی سرویس) از آن رفت.

تنها ردِ باقی‌مانده از آدرس قدیمی `AppSettings.LegacyStoreUrl` است که عمداً مانده: کارش دقیقاً
برعکس اتصال به سرور است — نصب‌های قدیمی را از آن آدرس **جدا** و به Pages منتقل می‌کند.

**تست UI (دستی — با کاربر)**: بعد از فعال شدن Pages، تب Store باید هر ۴ بسته را با آیکن بیاورد،
جستجو کار کند، نصب بدون خطا تمام شود و پلاگین بعد از ری‌استارت لود شود؛ و با اینترنت قطع، خطای
تمیز بدهد نه کرش. چک‌لیستش انجام شد.

### حذف کامل سرور فروشگاه (۱۶ شهریور ۱۴۰۵)

بعد از اینکه Pages پایدار جواب داد، هم کد سرور و هم خود سرویس برداشته شدند:

- پروژه‌ی `src/PlugLauncher.Store` (ASP.NET Core Minimal API: `Program.cs`، `PackageStore`،
  `PackageValidator`، `IndexPage`) حذف و از `PlugLauncher.slnx` بیرون آمد. سولوشن الان سه پروژه
  دارد: App، Core، Contracts. خروجی‌های `publish/store` هم پاک شدند.
- روی VPS: `pluglauncher-store.service` استاپ/دیسیبل و یونیتش حذف شد، بلوک
  `location /pluglauncher/` از `sites-enabled/hokm-server` برداشته و nginx ریلود شد،
  `/opt/pluglauncher-store`، `/var/lib/pluglauncher-store` و `/etc/pluglauncher-store.env` پاک و
  کاربر سیستمی `pluglauncher` حذف شد. پورت ۵۱۰۰ دیگر listener ندارد.
- قبل از حذف یک پشتیبان کامل (داده + یونیت + env + کانفیگ nginx) در
  `/root/pluglauncher-store-backup-20260907.tar.gz` گذاشته شد.
- آدرس قدیمی حالا به `index.html` ی سایت اصلی می‌رسد (۲۰۰ با `text/html`)، نه ۴۰۴. یعنی
  اگر نصبی هنوز آن آدرس را داشته باشد جواب JSON نمی‌گیرد و خطای پارس می‌دهد — به همین دلیل
  مهاجرت `LegacyStoreUrl` در `SettingsStore.Load` همچنان لازم است و نگه داشته شد.

### سه پلاگین جدید: SSH Hosts، Dev Toolbox، System (۱۶ شهریور ۱۴۰۵)

هر سه همراه برنامه هستند (`plugins/` که با csproj کنار exe کپی می‌شود) و آیکن‌شان با همان دستور
بقیه‌ی ست در `tools/make-icons.ps1` کشیده شد.

- **SSH Hosts** (`ssh`) — `~/.ssh/config` را می‌خواند، `Include` را هم دنبال می‌کند و بلوک‌های
  الگودار (`Host *`) را کنار می‌گذارد چون هدف اتصال نیستند. Enter یک ترمینال با `ssh <alias>` باز
  می‌کند؛ Windows Terminal اگر باشد وگرنه PowerShell. فایل با مهر زمانی کش می‌شود، پس ویرایش کانفیگ
  ری‌استارت نمی‌خواهد. آدرسی که در کانفیگ نیست (`root@10.0.0.5`) هم قابل اتصال است.
  چون هدف روی خط فرمان می‌نشیند، هر کاراکتری بیرون از `[A-Za-z0-9._-@:%+/[]]` رد می‌شود نه escape.
- **Dev Toolbox** (`dev`) — uuid، base64 (+base64url)، url encode/decode، sha256/sha1/md5،
  epoch↔تاریخ، pretty/minify کردن JSON، دیکد JWT (با یک ردیف صریح که امضا بررسی نشده)، بایت
  تصادفی و slug/snake/SCREAMING. Enter کپی می‌کند. تایپ خالیِ `dev` فهرست دستورها را با
  `ReplaceQuery` می‌آورد، همان الگوی پلاگین رمز.
- **System** (`sys`) — قفل/خواب/ری‌استارت/خاموش/خروج، خالی کردن سطل، ری‌استارت Explorer، فلاش DNS
  (با UAC)، کنسول‌های msc و cpl، صفحه‌های `ms-settings:` و چند پوشه‌ی پرکاربرد؛ به‌علاوه ردیف‌های
  خواندنی (uptime، RAM از `GlobalMemoryStatusEx`، فضای دیسک، IP، مشخصات ماشین) که Enter مقدارشان
  را کپی می‌کند. هر کاری که کار ذخیره‌نشده را از بین می‌برد اول با MessageBox می‌پرسد.
  در امتیازدهی، تطابق **دقیق** با تگ از «زیررشته در عنوان» بالاتر است وگرنه `sys ram` به‌جای ردیف
  حافظه، «Programs and Features» را اول می‌آورد (چون «Prog-ram-s»).

تست: یک هارنس موقت بیرون از مخزن (کنسول net10.0-windows با UseWPF، ارجاع به Core) هر سه را با
`CsxPluginLoader` کامپایل کرد و ~۳۰ کوئری رویشان اجرا شد — بدون خطا. علت WPF بودنش این است که
پلاگین‌ها `System.Windows.Clipboard` و `MessageBox` را صدا می‌زنند و در یک کنسول ساده کامپایل
نمی‌شوند، در حالی که در میزبان واقعی TPA این اسمبلی‌ها را دارد.

### زیرساخت ریلیز (۱۶ شهریور ۱۴۰۵)

نسخه‌ی ۱.۰.۰ آماده‌ی انتشار شد. تصمیم‌ها:

- **نسخه فقط در `Directory.Build.props`** است و هر سه پروژه از آن ارث می‌برند. هم
  `build-release.ps1` و هم workflow همان‌جا را می‌خوانند و تگ را با آن مقایسه می‌کنند، پس
  `v1.0.1` نمی‌تواند exe ی ۱.۰.۰ منتشر کند. خواندن با `SelectSingleNode` است نه
  `.Project.PropertyGroup.Version`، چون اضافه شدن یک PropertyGroup دوم آن را بی‌سروصدا آرایه می‌کند.
- **فقط framework-dependent** منتشر می‌شود؛ کاربر یک‌بار .NET 10 Desktop Runtime را نصب می‌کند.
  خروجی: ۳.۷MB فشرده، ۳۵ فایل. با `SatelliteResourceLanguages=en` منابع ترجمه‌شده‌ی Roslyn حذف
  شدند و همین ۵.۸MB/۸۷ فایل را به ۳.۷MB/۳۵ فایل رساند.
- **آیکن برنامه** (`make-app-icon.ps1`) — نکته‌ی مهم: `System.Drawing.Icon` که `NotifyIcon` از
  آن استفاده می‌کند فریم PNG داخل ico را **نمی‌خواند** (اکسپلورر می‌خواند). پس همه‌ی سایزهایی که
  خود برنامه لود می‌کند DIB سی‌ودو بیتی نوشته می‌شوند و فقط فریم ۲۵۶ PNG است. نسخه‌ی اول همه را
  PNG نوشت و `Icon.ToBitmap()` با «Requested range extends past the end of the array» افتاد.
- **GitHub Actions**: `release.yml` روی تگ `v*` (بیلد، چک تگ/نسخه، برداشتن متن ریلیز از
  CHANGELOG، ساخت Release با `gh` — نه اکشن شخص ثالث) و `build.yml` روی هر پوش به master تا
  کامیت خراب قبل از تگ خوردن گیر بیفتد.
- **README کامل انگلیسی** شد، با بخش دانلود، لینک مستقیم رانتایم و توضیح SmartScreen (فایل
  امضا نشده است).

### لایسنس و ست پلاگین پیش‌فرض (۱۶ شهریور ۱۴۰۵)

- **MIT** اضافه شد (`LICENSE`). تا قبل از آن مخزن عمومی بود ولی بدون لایسنس، یعنی از نظر
  حقوقی «تمام حقوق محفوظ» — نه fork، نه استفاده، نه مشارکت.
- **فقط سه پلاگین همراه برنامه می‌آیند**: calculator، password، programs. تا قبل از این،
  گلاب `plugins\**\*` در csproj هر پوشه‌ای را کنار exe می‌گذاشت، پس هر ۷ پلاگین پیش‌فرض فعال
  بودند. حالا سه مسیر صریح نوشته شده و اضافه کردن یک خط یعنی «برای همه نصب شود».
  خروجی ریلیز از ۳۵ به ۲۳ فایل رسید.
- **بقیه فقط در فروشگاه‌اند**: steam-games از قبل بود، ssh-hosts و dev-toolbox و system هم
  منتشر شدند (index الان ۷ بسته دارد). انتشار در فروشگاه چیزی را خودکار نصب نمی‌کند؛
  `index.json` صرفاً کاتالوگ است و تا کاربر Install نزند دانلودی انجام نمی‌شود.
- سه پلاگین همراه، در فروشگاه هم هستند و این عمدی است: `PluginDiscovery.Discover()` ریشه‌ی
  کاربر را بر ریشه‌ی کنار exe ترجیح می‌دهد و نسخه‌ی تکراری را با یک warning رد می‌کند، پس نصب
  از فروشگاه راهِ به‌روز کردن یک پلاگین همراه بدون منتظر ماندن برای ریلیز بعدی است.

### تعارض کلیدواژه و پلاگین چیدمان کیبورد (۱۶ شهریور ۱۴۰۵)

- **کلیدواژه مالک دارد.** قبلاً دو پلاگین با یک کلیدواژه هر دو صدا زده می‌شدند و نتایجشان قاطی
  می‌شد بدون هیچ توضیحی. حالا قدیمی‌ترین (بر اساس `Directory.GetCreationTimeUtc` پوشه، با
  مرتب‌سازی دوم روی `Id` چون در نصب تازه همه‌ی پوشه‌ها یک زمان دارند) نگه می‌دارد و بقیه
  `PluginState.Conflicted` می‌شوند و **اصلاً کامپایل نمی‌شوند**.
- یک پاس از قدیمی به جدید کافی است: پلاگینی که می‌بازد بقیه‌ی کلیدواژه‌هایش را هم claim نمی‌کند،
  پس پلاگین سومی که فقط با *او* تعارض داشت آزاد می‌ماند. با تست ثابت شد (old=zz، mid=zz+yy،
  new=yy → فقط mid کنار می‌رود).
- اطلاع‌رسانی با بالن سینی است نه MessageBox، چون این وضعیت تا عوض شدن `plugin.json` می‌ماند و
  دیالوگ مودال در هر اجرا آزار می‌شد. جزئیات در تنظیمات با نشان نارنجی «Keyword taken».
- **باگ جدول کیبورد.** جدول هاردکد `KeyboardLayout.cs` از روی تصویرهای رایج کیبورد فارسی بود نه
  `kbdfa` که ویندوز واقعاً دارد، و در ۹ کلید فرق داشت: `پ` روی `\` است نه `m`، و `m` می‌دهد `ئ`.
  یعنی `chrome` با فارسیِ جامانده می‌شد `زاقخئث` و برنامه آن را `chrose` می‌خواند. جدول با
  پرسیدن مستقیم از ویندوز (`VkKeyScanExW` + `ToUnicodeEx`) بازنویسی و تست شد — ۳۲ کلید و ۸ حرف
  shift دار، همه منطبق.
- **پلاگین Keyboard Layout** (`kb`): هیچ چیزش مخصوص فارسی نیست. لیست چیدمان‌ها را از
  `GetKeyboardLayoutList` می‌گیرد و برای هر جفت، تبدیل را از خود ویندوز می‌پرسد. اگر کاربر
  چیدمان سومی نصب کند، بدون تغییر کد پیشنهادش می‌دهد.
- تله‌ی مهم: `DllImport` پیش‌فرض `CharSet.Ansi` است. بدون `CharSet = CharSet.Unicode`، هم
  `VkKeyScanEx` حرف فارسی را `?` می‌گیرد و هم بافر UTF-16 ی `ToUnicodeEx` بایت‌به‌بایت خوانده
  می‌شود و «سلام» درمی‌آید `3D'E` (بایت‌های پایینِ همان کد‌پوینت‌ها). اولین اجرای تست دقیقاً همین
  را داد.

### بررسی خودکار به‌روزرسانی (۱۶ شهریور ۱۴۰۵)

- برنامه هیچ راهی نداشت بگوید نسخه‌ی جدیدی آمده؛ تنها راه سر زدن به خود مخزن بود. حالا روزی
  یک‌بار `api.github.com/repos/.../releases/latest` را می‌گیرد و تگ را با نسخه‌ی در حال اجرا
  مقایسه می‌کند.
- **تله‌ی `Version` با سه جزء.** `new Version("1.3.0")` مقدار `Revision = -1` می‌دهد و از
  `1.3.0.0` (که `AssemblyName.Version` برمی‌گرداند) **کوچک‌تر** حساب می‌شود. بدون نرمال‌سازی به
  چهار جزء، برنامه هر بار همان نسخه‌ی خودش را «جدید» اعلام می‌کرد. تست جداگانه دارد.
- **`User-Agent` اجباری است**؛ API گیت‌هاب بدون آن ۴۰۳ می‌دهد.
- «نتوانستم به گیت‌هاب برسم» از «به‌روز هستی» جدا نگه داشته شده (`UpdateCheckResult.Succeeded`).
  فقط بررسی موفق در `lastUpdateCheckUtc` ثبت می‌شود، وگرنه کسی که موقع بوت آفلاین است تا یک روز
  دیگر هم بررسی نمی‌شد.
- تایم‌اوت `HttpClient` هم `TaskCanceledException` می‌دهد، پس `catch (Exception)` نوشته شده و
  لغو واقعی با `ThrowIfCancellationRequested` جدا می‌شود — وگرنه یک تایم‌اوت ساده به بیرون
  پرت می‌شد و استارتاپ را می‌شکست.
- نشانه دو تاست: بالن سینی (کلیک → صفحه‌ی ریلیز) و یک نقطه کنار «Settings» که تا بسته شدن برنامه
  می‌ماند، چون بالن ده ثانیه بیشتر نمی‌ماند و به‌روزرسانیِ پیداشده در نبودِ کاربر اصلاً دیده نمی‌شد.
- **دانلود و نصب خودکار عمداً نیست**: نصب با installer و باز کردن zip دو مسیر متفاوت‌اند و برنامه
  نمی‌داند کاربر با کدام آمده. دکمه صفحه‌ی ریلیز را باز می‌کند و همان‌جا تمام.

### پلاگین Disk Usage و نوار پشت ردیف (۱۷ شهریور ۱۴۰۵)

- **`PluginResult.Fraction`** (۰ تا ۱) اضافه شد و لانچر آن را به شکل نوار کم‌رنگ پشت ردیف می‌کشد.
  پیاده‌سازی با دو ستون ستاره‌ای Grid است (`GridLength(f, Star)` و `GridLength(1-f, Star)`) که
  ستاره‌ی صفر ستون را جمع می‌کند، پس ۰ و ۱ بدون حالت خاص درست می‌افتند. تغییر قرارداد یعنی هش
  کش همه‌ی پلاگین‌ها عوض می‌شود و یک‌بار دوباره کامپایل می‌شوند.
- **پلاگین `du`**: یک پاس روی پوشه که فقط جمع نگه می‌دارد — بایت به ازای فرزند سطح اول، به ازای
  پسوند، و ۸ فایل بزرگ هر پسوند. اندازه‌گیری روی همین سیستم: کل `C:\` با ۴۶۰ هزار فایل در ۱۳ ثانیه،
  رشد heap صفر. پروفایل کاربر ۴ ثانیه، Documents زیر یک ثانیه.
- مهلت کوئری ۳ ثانیه است. پلاگین تا ۲ ثانیه منتظر اسکن می‌ماند؛ اگر تمام نشد یک ردیف «Scanning…
  N files so far» می‌دهد و اسکن در پس‌زمینه ادامه می‌یابد. این ردیف `RefreshAfterMs` دارد، پس
  لیست خودش تازه می‌شود (پایین را ببینید). Enter هم به‌عنوان fallback کار می‌کند: `ReplaceQuery`
  اش همان متن با یک فاصله‌ی انتهایی toggle شده است، چون متن یکسان `TextChanged` را نمی‌زند.
- `EnumerationOptions.AttributesToSkip` پیش‌فرض Hidden و System را هم رد می‌کند که یعنی AppData
  (بزرگ‌ترین چیز اکثر پروفایل‌ها) حذف می‌شد. فقط `ReparsePoint` رد می‌شود تا junction و
  placeholder های OneDrive دو بار شمرده نشوند یا حلقه نیفتند.
- **جستجوی فایل ساخته نشد**، عمداً: Windows Search به `System.Data.OleDb` نیاز دارد که در .NET 10
  نیست و `references` ی مانیفست فقط موقع کامپایل کار می‌کند (resolver زمان اجرا نداریم). تصمیم:
  ساده بماند.
- پلاگین چون به `Fraction` وابسته است روی نسخه‌های قبل از ۱.۵.۰ کامپایل نمی‌شود؛ در توضیحش نوشته
  شده و باید **بعد از** ریلیز ۱.۵.۰ به فروشگاه پوش شود.

### تازه‌سازی خودکار و راهنمای مانیفست (۱۷ شهریور ۱۴۰۵)

- **`PluginResult.RefreshAfterMs`**: پلاگین می‌گوید «کارم تمام نشده، فلان‌قدر بعد دوباره بپرس».
  `MainWindow.ScheduleRefresh` یک `DispatcherTimer` جدا از دیبانس است. سه سقف روی آن هست چون
  یک پلاگین معیوب می‌تواند همیشه این را بگذارد: کف ۲۵۰ms، بودجه‌ی ۲ دقیقه برای هر متنِ تایپ‌شده
  (`_refreshUntil`، با هر `TextChanged` شارژ می‌شود)، و ایست کامل با بسته شدن پنجره. سومی
  مهم‌ترین است — پنجره فقط وقتی باز است که کاربر جلویش نشسته، پس هیچ حلقه‌ای در پس‌زمینه نمی‌ماند.
  گزینه‌ی «یک کال در دقیقه» بررسی و رد شد: اسکن `C:\` سیزده ثانیه است و آن سقف عملاً قابلیت را
  از کار می‌انداخت، ضمن اینکه یک پلاگین معیوب را متوقف هم نمی‌کرد و فقط کندش می‌کرد.
- **`manifest.usage`**: فهرست `{ example, description }` در `plugin.json`. موتور در
  `PluginEngine.UsageRowsFor` آن‌ها را فقط وقتی اضافه می‌کند که کلیدواژه تایپ شده و `Search` خالی
  باشد؛ **بعد از** `Take(MaxResults)` تا نه جای نتیجه را بگیرد نه قربانی سقف شود. حداکثر ۶ خط.
  هر خط `ReplaceQuery` دارد پس Enter آن را در باکس می‌گذارد و اجرا نمی‌کند.
- به `du`، `sys`، `ssh`، `kb` و `st` اضافه شد. به `dev` و `pw` **نه**، چون خودشان همین کار را
  داخل اسکریپت می‌کنند؛ بعداً می‌شود آن کد را حذف و به مانیفست منتقل کرد.
- `usage` برای هسته‌های قدیمی‌تر بی‌خطر است: `System.Text.Json` فیلد ناشناخته را نادیده می‌گیرد،
  پس این چهار پلاگین را می‌شود بدون وابستگی به نسخه‌ی ۱.۵.۰ منتشر کرد. فقط `du` است که به
  `Fraction` و `RefreshAfterMs` نیاز دارد.

### پیشنهاد گام‌به‌گام کامند (۱۷ شهریور ۱۴۰۵)

- `usage` مانیفست فقط لحظه‌ی اول را حل می‌کرد. چیزی که کم بود پیشنهاد در **هر مرحله** بود:
  کاربر `du C:\` را دیده بود ولی نمی‌دانست می‌تواند `ext` به آخرش اضافه کند.
- دانش «چه چیزی می‌تواند بعدی بیاید» ذاتاً مال پلاگین است (کدام پسوندها واقعاً در آن پوشه هستند)،
  پس هسته نمی‌تواند تولیدش کند. کار هسته فقط رندر کردن و پذیرفتن است.
- **در پلاگین**: نمای پوشه حالا ردیف «By extension» هم دارد؛ `ext` بدون آرگومان فهرست پسوندها را
  می‌دهد؛ `ext .z` فهرست را با پیشوند فیلتر می‌کند؛ `types Ar` به‌جای «kind پیدا نشد» فهرست را
  فیلتر می‌کند؛ و `TrySplitVerb` فعلِ نیمه‌تایپ‌شده را می‌گیرد (`du C:\ ty` → `du C:\ types`).
  محافظ آخری: فقط وقتی که مسیر کامل وجود نداشته باشد ولی مسیرِ بدون کلمه‌ی آخر وجود داشته باشد،
  تا پوشه‌ی واقعی به‌نام `e` برنده بماند.
- حالت مبهم: `.d` هم پسوند واقعی است هم شروع `.dll`. تصمیم: تطابق دقیق برنده است (قابل‌پیش‌بینی)،
  ولی دو پسوندِ بلندتر با همان پیشوند بالای فهرست فایل‌ها پیشنهاد می‌شوند تا از آنجا دست‌نیافتنی
  نباشند. امتیازها ۲۷۹/۲۷۸ برای پیشنهادها و ۲۷۰ به پایین برای فایل‌ها، چون سقف ۸ ردیف است.
- **در هسته**: `UpdateHint` قبلاً برای این ردیف‌ها غلط بود — عنوان ردیف را به آخر متن می‌چسباند و
  `du c:/ c:\` می‌ساخت. حالا اگر ردیف `ReplaceQuery` داشته باشد همان مبنای سایه است، و اگر
  ادامه‌ی متن تایپ‌شده نباشد هیچ سایه‌ای نشان داده نمی‌شود. Tab روی چنین ردیفی کل متن را جایگزین
  می‌کند، پس پذیرفتنِ پیشنهاد همیشه کار می‌کند حتی وقتی سایه‌ای نیست.
- سوءتفاهم درباره‌ی سقف نرخ: معماری event-based نیست. پلاگین هیچ‌وقت میزبان را صدا نمی‌زند؛
  میزبان از پلاگین می‌پرسد. `RefreshAfterMs` یک درخواست است نه یک کال، و سقف‌هایش همان سه‌تای
  بالاست.

### کارهای باز (اختیاری، فاز ۵)

- [ ] حذف پلاگین از داخل تب فروشگاه (الان فقط در تب «پلاگین‌های نصب‌شده» هست)
- [x] بررسی خودکار به‌روزرسانی در استارتاپ + نشانگر روی دکمه‌ی تنظیمات — انجام شد (۱۶ شهریور ۱۴۰۵)
- [ ] صفحه‌ی وب ساده برای مرور فروشگاه در مرورگر — روی VPS ساخته شده بود (۱۵ شهریور ۱۴۰۵) ولی
  موقع کوچ به GitHub Pages همراه نیامد. `build-static-store.ps1` فقط `index.json` و `packages/`
  می‌سازد، پس ریشه‌ی سایت الان همان JSON خام را برمی‌گرداند. تیکش اشتباه مانده بود.
- [ ] امضای دیجیتال بسته‌ها (فراتر از sha256) اگر انتشار از کلید شما خارج شد
- [x] **میزبانی فروشگاه روی Git** — GitHub Pages روی برنچ `gh-pages` همین مخزن
  (`https://mul83rry.github.io/PlugLauncher/`)؛ کلاینت، اسکریپت انتشار و مستندات منتقل شدند.
  مخزن عمومی شد و Pages روشن است.

---

## فاز ۶ — پورت به مک و لینوکس

نقطه‌ی شروع (۱۷ شهریور ۱۴۰۵): `Contracts` و `Core` هر دو `net10.0` خالی‌اند و یک `DllImport` هم
ندارند — موتور، کامپایل Roslyn، کش، فروشگاه و آپدیت از همین حالا قابل حمل‌اند. چیزی که به ویندوز
گره خورده: پوسته‌ی WPF، سه فایل `Interop/`، کلید Run رجیستری، نصاب Inno، و چهار پلاگین.

سخت‌ترین نقطه UI نیست، هات‌کی سراسری است: روی مک اجازه‌ی Accessibility می‌خواهد و روی Wayland
اصلاً API ندارد. اگر آنجا جواب ندهد، لانچرِ هات‌کی‌محور روی لینوکس نصفه است.

### گام ۱ — کلیپ‌بورد از قرارداد بیاید، نه از WPF

- [x] `Clipboard.Copy` و `Clipboard.Text` در `PlugLauncher.Contracts`؛ میزبان با `Clipboard.Use`
  پیاده‌سازی را می‌گذارد. هفت پلاگین مستقیم `System.Windows.Clipboard` صدا می‌زدند — تنها
  وابستگی ویندوزی‌ای که در پلاگین‌های *غیرویندوزی* هم بود، چون از دل WPF می‌آمد.
- [x] حلقه‌ی «سه بار تلاش» که در هفت اسکریپت کپی شده بود، یک‌جا در میزبان.
- [x] هر هفت پلاگین منتقل و بسته‌بندی شدند. `calculator` و `password` نسخه گرفتند چون نسخه‌ی
  قبلی‌شان روی فروشگاه زنده است؛ بقیه هنوز منتشر نشده بودند.
- نکته‌ی سازگاری: پلاگینی که کپی می‌کند از این به بعد به هسته‌ی ۱.۵.۰ نیاز دارد. مانیفست فیلدی
  برای «حداقل نسخه‌ی هسته» ندارد؛ `du` و `curl` هم همین وضع را دارند. اگر قرار است فروشگاه این را
  بفهمد، یک فیلد `minCore` لازم است.

### گام ۱.۵ — پلاگین بگوید مال کجاست

- [x] `platforms` و `minCore` در `plugin.json`؛ `PluginSupport.Reject` تنها جایی است که تصمیم
  می‌گیرد، چون دو نفر می‌پرسند: موتور موقع لود و فروشگاه قبل از فعال کردن دکمه‌ی Install.
- [x] وضعیت جدید `PluginState.Unsupported` — نه قرمز، نارنجی: پلاگین خراب نیست، مال اینجا نیست.
- [x] فروشگاه: دلیل اول خط متا می‌آید و دکمه «Unavailable» می‌شود. `InstallFromStoreAsync` هم
  جداگانه جلویش را می‌گیرد.
- [x] `build-static-store.ps1` هر دو فیلد را به `index.json` می‌برد؛ `pack-plugin.ps1` مقدار غلط
  را همان موقع بسته‌بندی رد می‌کند.
- [x] هر ده مانیفست پر شد. چهار پلاگین همه‌جایی: `calculator`، `curl`، `dev-toolbox`، `password`.
  شش‌تای دیگر فعلاً ویندوزی‌اند؛ `disk-usage` بعد از گام ۴ همه‌جایی می‌شود.

### گام ۲ — لایه‌ی پلتفرم

- [x] پروژه‌ی `PlugLauncher.Platform` روی `net10.0` با `IHotkeys`، `IAutoStart`، `IOpener`.
  انتخاب پیاده‌سازی در زمان اجرا، نه با `#if`، چون یک بیلد باید هر سه جا اجرا شود.
- [x] `Hotkey` نامِ کلید را نگه می‌دارد نه کدش. ویندوز کد مجازی دارد، مک کد Carbon و لینوکس
  keysym؛ ترجمه کارِ همان سیستم است. نام‌های WPF («Space»، «D1»، «Oem3») هم خوانده می‌شوند تا
  هات‌کی ذخیره‌شده‌ی کسی از دست نرود.
- [x] ویندوز: `RegisterHotKey` روی **پنجره‌ی خودش** (message-only) نه پنجره‌ی اصلی — وگرنه لایه‌ی
  پلتفرم دوباره به UI گره می‌خورد. کلید Run و `explorer.exe /select,` هم منتقل شدند؛
  `StartupRegistration` و `Interop/GlobalHotkey` حذف شدند.
- [x] مک و لینوکس: `IAutoStart` و `IOpener` نوشته شدند (LaunchAgent plist و فایل `.desktop`،
  `open -R` و `xdg-open`). تست‌نشده — ماشینش نیست.
- [x] مک: `RegisterEventHotKey` (Carbon) — نوشته شد، **اجازه‌ی Accessibility لازم نشد**. آن اجازه
  مالِ `CGEventTap` است که کلِ کلیدهای سیستم را می‌بیند؛ کربن فقط همان یک ترکیبِ ثبت‌شده را
  می‌دهد و برای همین چیزی نمی‌پرسد. یعنی روی مک هیچ تیکی در تنظیمات امنیتی لازم نیست.
  - نصب هندلر و ثبت ترکیب دو کار جدا هستند: هندلر یک‌بار می‌نشیند و می‌ماند، ترکیب با هر تغییر
    عوض می‌شود — وگرنه عوض کردن هات‌کی از تنظیمات هربار یک هندلر تازه روی هم می‌چید.
  - `MacKeys`: جدول کد کلیدها. برخلاف ویندوز هیچ الگویی ندارد (`A` صفر است، `B` یازده) و صفر
    خودش کد معتبری است، پس نتیجه با `bool` برمی‌گردد نه با «صفر یعنی نشد». نام‌های ویندوزی هم
    خوانده می‌شوند چون همان `settings.json` روی مک هم باز می‌شود.
  - `MacWindowing`: روی مک پنجره جلو نمی‌آید، **برنامه** جلو می‌آید. بدون
    `activateIgnoringOtherApps:` پنجره دیده می‌شود ولی کیبورد دست برنامه‌ی قبلی می‌ماند.
  - کل `TryRegister` داخل `try` است: اگر نام یکی از چهار تابع کربن غلط باشد، کاربر یک جمله
    می‌بیند نه یک کرش.
  - **روی مک تست شد و کار کرد** (۱۷ شهریور ۱۴۰۵).
- [ ] لینوکس: `XGrabKey` روی X11
- [x] `tools/build-mac.sh` — بسته‌ی `PlugLauncher.app`. بسته‌بندی اینجا تزئین نیست: کربن هات‌کی را
  فقط به پروسه‌ای می‌دهد که ویندو-سرور آن را «برنامه» بداند، و یک باینری که از ترمینال اجرا شود
  چنین چیزی نیست — پس `dotnet run` می‌تواند لانچری بدهد که هیچ‌وقت باز نمی‌شود، به دلیلی که هیچ
  ربطی به کد هات‌کی ندارد. self-contained، چون کسی که اولین بار امتحان می‌کند نباید وقتش صرف
  پیدا کردن رانتایم شود.
- [x] `build.yml` روی هر سه سیستم بیلد می‌کند و روی مک بسته را هم می‌سازد. رانرها P/Invokeِ
  اشتباه را نمی‌گیرند (آن فقط موقع صدا زدن معلوم می‌شود) ولی شایع‌ترین راهِ پوسیدنِ یک پورت را
  می‌گیرند: مسیری با بک‌اسلش، یا فایلی که بی‌صدا دیگر کپی نمی‌شود.
- [x] مسیرهای `PlugLauncher.App.csproj` اسلش رو به جلو شدند. `ApplicationIcon` همان‌طور که هست به
  کامپایلر می‌رود و بیرون ویندوز بک‌اسلش جزئی از نامِ فایل حساب می‌شود. با کراس-بیلدِ
  `-r osx-arm64` از روی ویندوز تست شد: apphost مک-او arm64، ۲۳۹ فایل، ۱۲۰ مگابایت، هر سه پلاگین
  سر جایشان.
- [x] رفتار وقتی هات‌کی ثبت نمی‌شود: `NoHotkeys` دلیل را برمی‌گرداند و پنجره همان جمله را نشان
  می‌دهد. لانچری که باز نمی‌شود باید بگوید چرا، نه اینکه لال بماند.

### گام ۳ — پوسته‌ی Avalonia به‌جای WPF

- [x] یک پوسته برای هر سه OS، نه دو تا. `PlugLauncher.App` روی `net10.0` رفت و پسوند `-windows`
  از آخرین پروژه هم برداشته شد. WPF بازنشسته شد.
- [x] `TrayIcon` آوالونیا به‌جای `NotifyIcon` وین‌فرمز.
- [x] بلور پنجره با `TransparencyLevelHint`؛ اگر سیستم برنداشت، پس‌زمینه‌ی تخت. تصمیم در
  `OnOpened` از روی `ActualTransparencyLevel` گرفته می‌شود، نه با حدس زدنِ نسخه‌ی ویندوز.
- [x] تک‌نمونه بودن با یک فایل قفل، نه `Mutex` نام‌دار. اگر برنامه کشته شود، سیستم‌عامل خودش
  فایل را رها می‌کند و اجرای بعدی گیر نمی‌افتد.
- [x] بالنِ سینی جایش را به یک پنجره‌ی کوچک گوشه‌ی صفحه داد. بالن فقط در ویندوز هست و
  `TrayIcon` آوالونیا اصلاً راهی برای نشان دادنش نمی‌دهد.
- [x] `MessageBox` هم فقط در ویندوز هست: `Dialog` با یک حلقه‌ی پیام تودرتو (`DispatcherFrame`)
  نوشته شد تا امضای هم‌زمانِ صداکننده‌ها دست‌نخورده بماند — از جمله `Ask.Confirm` که قرارداد
  پلاگین‌هاست.
- [x] `Ask.Confirm` در `Contracts`. پلاگین `system` مستقیم `System.Windows.MessageBox` صدا
  می‌زد؛ همان یک خط، پلاگین را به یک کتابخانه‌ی UI می‌بست.
- [x] تمِ کاربر از `theme.xaml` به `theme.json` رفت. فایل قبلی مستقیم به موتور تمِ WPF داده
  می‌شد و آن موتور دیگر نیست؛ کلیدها عوض نشدند.
- [x] `ForegroundHelper` و آیکنِ داخل exe به لایه‌ی پلتفرم رفتند (`IWindowing`، `IIcons`).
  `WindowEffects` حذف شد — کارش را خود آوالونیا می‌کند.
- [x] نصاب به‌جای Desktop Runtime دنبال خودِ Runtime می‌گردد. بدون WPF، بسته‌ی Desktop لازم
  نیست و کسی که آن را دارد این را هم دارد.
- روی ویندوز تست شد: هات‌کی، جستجو، سایه‌ی تکمیل، کپی و خواندنِ کلیپ‌بورد، پیستِ چندخطی،
  تنظیمات، فروشگاه با آیکن‌ها، پیام گوشه‌ی صفحه، دیالوگ، تک‌نمونه بودن، آیکن سینی.
- مک تست شد و کار کرد. لینوکس هنوز هات‌کی ندارد و بدون آن لانچر راهی برای باز شدن ندارد.

### گام ۴ — پلاگین‌ها

- [x] فیلد `platforms` در مانیفست + فیلتر در فروشگاه — در گام ۱.۵ انجام شده بود، تیکش جا مانده
  بود. فروشگاه بسته‌ی ناسازگار را پنهان نمی‌کند؛ دلیلش را می‌نویسد و دکمه‌ی نصب را خاموش
  می‌کند. بسته‌ای که ناپدید شود کاربر را سردرگم می‌کند، بسته‌ای که بگوید «مال ویندوز است»
  خودش را توضیح می‌دهد.
- [x] `Shell.Open` و `Shell.Reveal` در `Contracts`، سومی بعد از `Clipboard` و `Ask`. پلاگین‌ها
  `IOpener` را نمی‌بینند — فقط `Contracts` را ارجاع می‌دهند — پس «از `IOpener` رد شود» در عمل
  یعنی یک ورودی تازه در قرارداد، نه یک جایگزینی در اسکریپت.
- [x] `disk-usage` همه‌جایی شد: `explorer.exe /select,` رفت، جداکننده‌ی مسیر از
  `Path.DirectorySeparatorChar` می‌آید، `C:` → `C:\` فقط روی ویندوز معنی دارد، و فهرست درایوها
  فایل‌سیستم‌های مجازی لینوکس (`/proc`، `/sys`، tmpfs) را رد می‌کند. برچسبِ درایو جداگانه
  پرسیده می‌شود، چون بیرون ویندوز ممکن است اصلاً جواب ندهد و نبودِ برچسب دلیلِ انداختن درایو
  نیست.
- [x] `ssh-hosts` همه‌جایی شد. پارس `~/.ssh/config` از اول قابل حمل بود؛ تنها تکه‌ی ویندوزی
  «یک پنجره باز کن و داخلش ssh بزن» بود: `wt.exe`/PowerShell روی ویندوز، `osascript` برای
  Terminal.app روی مک، و هفت ترمینال شناخته‌شده روی لینوکس که به‌ترتیب امتحان می‌شوند.
- [x] `steam-games` همه‌جایی شد. کتابخانه‌ی Steam همه‌جا یک شکل است (`libraryfolders.vdf` و یک
  `appmanifest` برای هر بازی)، فقط پیدا کردنِ خود پوشه‌ی Steam فرق می‌کند: رجیستری روی ویندوز،
  `~/Library/Application Support/Steam` روی مک، و سه مسیر روی لینوکس (با فلت‌پک).
- [ ] `programs` و `system` ویندوزی می‌مانند: اولی منبع داده‌ی کاملاً متفاوتی می‌خواهد
  (`/Applications` و `*.desktop` به‌جای منوی Start) و دومی یک جدول دستور جداگانه برای هر
  سیستم. هیچ‌کدام ترجمه‌ی مکانیکی نیست.

روی ویندوز تست شد: `du` (هر دو درایو با نوار و درصد)، `du ~\...` (بازشدن `~`)، Enter روی سرصفحه
(اکسپلورر همان پوشه را باز کرد)، Enter روی یک فایل (اکسپلورر با همان فایل انتخاب‌شده)، `ssh`
(چهار هاست از کانفیگ) و Enter روی یک هدف دستی (تبِ ترمینال با عنوان درست و خطای DNS).

`steam-games` روی این ماشین فقط لود و اجرا شد — Steam نصب نیست، پس مسیرهای تازه‌ی پیدا کردن
پوشه‌ی Steam و اجرای بازی هیچ‌کدام تست نشده‌اند.

### گام ۵ — بیلد و انتشار

- [ ] سه RID در **ورک‌فلوی انتشار**؛ نصاب Inno فقط ویندوز می‌ماند.
  ورک‌فلوی `build` همین حالا هر سه را می‌سازد، ولی `release` هنوز فقط `windows-latest` است و تنها
  چیزی که به Releases می‌رود همان zip و setup ویندوز است.
- [x] مک: بسته‌ی `.app` — `tools/build-mac.sh`، و روی رانرِ مک هم ساخته می‌شود.
- [ ] مک: امضا و نوتاریزیشن. تا وقتی نباشد بسته فقط روی ماشینی که خودش ساخته اجرا می‌شود؛ اگر
  کسی آن را دانلود کند Gatekeeper جلویش را می‌گیرد. حساب توسعه‌دهنده‌ی اپل می‌خواهد، پس تصمیم
  است نه کار.
